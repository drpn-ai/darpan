
# gradle/build files
build
.gradle
/framework/dependencies

# build generated files
/moqui*.war
/Save*.zip

# runtime directory (separate repository so ignore directory entirely)
/runtime
/execwartmp

# IntelliJ IDEA files
.idea
out
*.ipr
*.iws
*.iml
*.jar
*.class

# Eclipse files (and some general ones also used by Eclipse)
.metadata
bin
tmp
*.tmp
*.bak
*.swp
*~.nib
moqui_logs/
local.properties
.settings
.loadpath

# NetBeans files
nbproject/private
nbbuild
.nb-gradle
nbdist
nbactions.xml
nb-configuration.xml

# VSCode files
.vscode

# OSX auto files
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows auto files
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux auto files
*~

# .github/workflows/ is intentionally TRACKED — backend CI restored (MACH audit P0 #2).
# (Previously ignored by commit 82a1be1 "chore: stop tracking github workflows".)

# Python bytecode from scripts/ (select_tests.py and friends)
__pycache__/
*.pyc
